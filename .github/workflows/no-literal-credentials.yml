name: no-literal-credentials

on:
  push:
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: apps/admin 리터럴 자격증명 금지
        run: ./scripts/check-admin-credentials.sh
